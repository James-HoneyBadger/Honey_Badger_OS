# Honey Badger OS Build Configuration
# ARM64 Linux Distribution with XFCE Desktop

DISTRO_NAME="Honey Badger OS"
DISTRO_VERSION="1.0"
DISTRO_CODENAME="fearless"
DISTRO_DESCRIPTION="ARM64 Linux Distribution for Developers"

# Architecture and Kernel
ARCH="arm64"
KERNEL_VERSION="6.11"  # Latest stable kernel as of Nov 2024
KERNEL_PACKAGE="linux-image-generic"

# Base system
BASE_SYSTEM="debian"
DEBIAN_RELEASE="bookworm"
MIRROR="http://deb.debian.org/debian"
SECURITY_MIRROR="http://security.debian.org/debian-security"

# Package manager
PACKAGE_MANAGER="apt"
APT_SOURCES="/etc/apt/sources.list"

# Desktop Environment
DESKTOP_ENVIRONMENT="xfce4"
DISPLAY_MANAGER="lightdm"

# Build directories
BUILD_DIR="/tmp/honey-badger-build"
CONFIG_DIR="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
SCRIPTS_DIR="$(dirname "$CONFIG_DIR")/scripts"
LIVE_BUILD_DIR="$BUILD_DIR/live-build"

# Output
ISO_NAME="honey-badger-os-${DISTRO_VERSION}-${ARCH}.iso"
ISO_OUTPUT_DIR="$(dirname "$(dirname "$CONFIG_DIR")")/ISOs"

# Live system user
LIVE_USER="honeybadger"
LIVE_USER_FULLNAME="Honey Badger User"
LIVE_USER_PASSWORD="live"

# System settings
HOSTNAME="honeybadger-os"
TIMEZONE="UTC"
LOCALE="en_US.UTF-8"
KEYBOARD="us"

# Default applications
DEFAULT_EDITOR="nano"
VISUAL_EDITOR="nano"
EDITOR="nano"